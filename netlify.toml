[build]

  publish = "."

  functions = "functions"

  environment = { NODE_VERSION = "20" } 


[functions]

  node_bundler = "esbuild" 

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true 
